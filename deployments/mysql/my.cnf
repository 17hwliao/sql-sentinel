[mysqld]
# 两个实例挂载同一份配置，参数一致是快照与测量可比的前提。
# 任何一侧单独调参都会让 A/B 结果失去意义。

default-time-zone = '+00:00'

innodb_buffer_pool_size        = 512M
innodb_redo_log_capacity       = 256M
innodb_flush_log_at_trx_commit = 2

max_connections    = 64
performance_schema = OFF
