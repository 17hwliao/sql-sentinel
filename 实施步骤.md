# SQL Sentinel 实施步骤

> 当前阶段：阶段 0，证据层 Spike。只有 Spike 通过，才开始 Agent 编排与 GitHub 集成。

> Timebox：阶段 0 最多 7 天；若目标 500 万行不稳定则固定为 100 万行。双实例不稳定时才可切换为“同一快照还原 + 重启 + 相同预热”的单实例协议；若只能做统计信息注入，则报告最高只能为 L1，不能宣称实际性能收益。噪声需独立标定至少 20 轮，Case 对比至少 5 轮。仍不能跑通则重估选题。

## 阶段 0：证据层 Spike 任务清单

### A. 固定实验环境

- [ ] 决定 MySQL 版本（最低要求 8.0.18，以支持 `EXPLAIN ANALYZE`）。
- [ ] 记录 CPU、内存、磁盘、Docker 版本、MySQL 参数、InnoDB Buffer Pool 大小。
- [ ] 创建 `docker-compose.yml`，启动 baseline/candidate 两个 MySQL 实例。
- [ ] 创建一键重建、导入数据、执行基准、导出报告的脚本。

### B. 设计基准 Schema 与数据

- [ ] 设计 `orders` 表：主键、`user_id`、`status`、`created_at`、金额、可空字段和少量 payload。
- [ ] 设计数据分布：高基数 user_id、低基数 status、Zipf 热点、NULL 比例、时间分布。
- [ ] 编写 Go 造数器；先导入 100 万行，成功后评估是否扩至 500 万行。
- [ ] 为每次数据生成记录 `dataset_version`、随机种子和参数。

### C. 设计验证 Case

- [ ] Case 1：高选择性条件，候选索引应有显著收益。
- [ ] Case 2：低选择性条件，验证“全表扫描不一定是问题”。
- [ ] Case 2b：候选索引让读变快，但 N 并发写入的吞吐/锁等待代价超过约定阈值，结论应为“不建议建立”。
- [ ] Case 3：深分页/排序，比较游标分页或联合索引策略。
- [ ] Case 4：隐式类型转换或函数包裹索引列。
- [ ] Case 5：故意错误 SQL 改写，用于等价性校验应失败。

### D. 可信测量实现

- [ ] 每个固定环境先独立运行至少 20 轮噪声标定，记录 `noise_floor = MAD(latency) / median(latency)`；MAD 对长尾延迟更稳健。
- [ ] 每个 baseline/candidate Case 至少运行 5 轮，记录中位数、P95、错误率；噪声地板原则上应低于 10%。
- [ ] 只有中位数变化大于 `2 × noise_floor` 时才在报告中声称存在收益；否则标注为不显著。
- [ ] 分别收集 `EXPLAIN FORMAT=JSON` 与 `EXPLAIN ANALYZE`；前者是计划，后者是实际迭代器证据。
- [ ] 对 candidate 实例执行候选索引 DDL 和 `ANALYZE TABLE`，再重复相同测量。
- [ ] 使用独立 baseline/candidate 实例，不在同一实例反复增删索引对比。
- [ ] 验证任务串行化，避免 CPU、IO 和 Buffer Pool 相互污染。
- [ ] 对 SQL 改写做结果集等价性校验；对候选索引测量单写及 N 并发 INSERT/UPDATE 的吞吐、P95、锁等待和索引空间代价。

### E. Spike 交付物与门槛

- [ ] `validation_result.json`：机器可读的环境、版本、Case、指标和 Plan 摘要。
- [ ] `spike_report.md`：解释一个有收益 Case、两个不同原因的无收益 Case（全扫更优、写入代价过高）和一个等价性失败 Case。
- [ ] 基线与候选对比可重复至少 5 轮且趋势稳定。
- [ ] 确认可行后，将测量代码抽象为 `Verifier` 模块。
- [ ] `make demo-stage-0`：一条命令输出有收益、全扫更优、写代价过高和等价性失败四类 Case 的报告。
- [ ] 顶层 `求职私有资料/`：记录已测指标与噪声/`EXPLAIN ANALYZE`/串行化追问；不提交至公开项目仓库。项目仓库的 `decisions/` 公开记录双实例与 `database/sql` 等关键取舍。

## 全局阶段 2：受限 Agent 核心

> Timebox：最多 10 天。第一档降级：只保留 `add_index` CandidateSpec、一个可验证 Case、Graph 取证/上限/降级，暂缓 SQL 改写、L3 数据和扩展规则。第二档降级：保留 Agent 的证据综合、自然语言解释和置信度判定，候选改由确定性规则模板生成，取消多轮自主取证与候选生成。最后才降为纯规则诊断报告，并同步取消“自主优化 Agent”定位。

- [ ] 定义 `Evidence`、`CandidateSpec`、`ValidationResult`、`Report` 结构。
- [ ] 实现 SQL Parser、输入白名单与静态规则。
- [ ] 实现 Schema、Index、ColumnType、Plan、Stats 只读 Tool。
- [ ] 实现 CandidateSpec 的 Go 校验器和 DDL 编译器。
- [ ] 实现 Eino Graph：取证、候选、验证、循环、上限与降级。
- [ ] 实现 L0–L3 置信度报告。
- [ ] 用固定 Case 集验证每条建议的证据关联。
- [ ] `make demo-stage-2`：展示“规则 → 取证 → CandidateSpec → Go 校验 → 验证 → 降级报告”的一次完整链路。
- [ ] 更新顶层 `求职私有资料/` 与公开的 `decisions/`。

## 全局阶段 3：工程闭环

> Timebox：最多 10 天。优先 Webhook 幂等、队列、任务状态、报告和审批 API；GitHub 评论集成卡住时允许使用已签名 Webhook fixture 演示，但 README 必须说明真实评论回写尚未完成。不要为解决集成问题引入前端、ES 或 gRPC。

- [ ] GitHub Webhook 验签和 Delivery ID 幂等。
- [ ] 队列与 Worker；影子库独占资源、验证串行。
- [ ] 任务状态机与重试策略；定义领取租约、心跳/超时和尝试版本。多 Verifier 扩容后才引入 fencing token 等跨节点保护。
- [ ] JWT、Casbin、CandidateSpec 人工审批。
- [ ] 回写 PR 评论和审计日志。
- [ ] Eino Callback、OTel、Prompt/模型/规则/数据集版本化。
- [ ] `make demo-stage-3`：回放同一个 Delivery ID 两次，确认只产生一个任务/报告；展示审批前后状态变化。
- [ ] 更新顶层 `求职私有资料/` 与公开的 `decisions/`。

## 后置扩展

- [ ] ES：历史实测数据的结构化检索、全文检索和聚合；只作为 Historical Prior。
- [ ] PgSQL：同一 Schema/数据/查询的跨引擎对照实验，产出 README 报告。
- [ ] Verifier 独立进程；只有确有同步进度与取消需求时才使用 gRPC。
